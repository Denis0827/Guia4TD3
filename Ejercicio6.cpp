#include <iostream>
#include <string>
#include <vector>
using namespace std;

vector<int> sublista_suma_maxima_aux(const vector<int> &v, int desde, int hasta) {
    if(desde == hasta-1) { 

    }
    else {
        
    }
}